theory ls {

  //declaration section
  int [] a;
  int left;
  int right;
  int e;

  constants {0, 1, -1}


  grammar {
    (a,left,bound);
    (a,right,bound);
    (e,a,=);
  }

  num_operations_per_clause_bound = 2;
  num_quantifiers_bound = 1;
}
